{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 tools/check_data_parity.py || echo '⚠️  data parity check failed — inline fallback, data.json and the index engine disagree (see tools/check_data_parity.py)'"
          }
        ]
      }
    ]
  }
}
